package org.example.Service;

import org.example.Model.Project;
import org.example.Model.Team;

public class TeamService {

    /*
    public Team createTeam(Project project, List<User> availableDevs) {
        if (!isValidTeamSize(project, availableDevs)) {
            throw new InvalidTeamSizeException();
        }
        if (!hasRequiredSkills(project, availableDevs)) {
            throw new SkillMismatchException();
        }
        // Autres règles métier
        return new Team(assignedDevs);
    }

    private boolean isValidTeamSize(Project project, List<User> devs) {
        // Implémenter la logique de vérification de la taille de l'équipe
    }

    private boolean hasRequiredSkills(Project project, List<User> devs) {
        // Implémenter la logique de vérification des compétences
    }*/

}
